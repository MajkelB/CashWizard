<resources>
    <!-- base set of styles that apply to all versions -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryTextColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
    </style>

    <!-- declare the theme name that's actually applied in the manifest file -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Title -->
    <style name="Title_Text" parent="AppTheme">
        <item name="android:textSize">24dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="Medium_Text" parent="AppTheme">
        <item name="android:textSize">20dp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="Medium_Low_Text" parent="AppTheme">
        <item name="android:textSize">18dp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="Normal_Text" parent="AppTheme">
        <item name="android:textSize">15dp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <!-- Title -->
    <style name="Big_Text" parent="AppTheme">
        <item name="android:textSize">30dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Switch -->

    <style name="CWSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/colorControlActivated</item>

        <!-- inactive track color (30% transparency) -->
<!--        <item name="android:colorForeground">@color/colorForeground</item>-->

<!--        <item name="android:colorControlActivated">@color/colorForeground</item>-->
        <item name="thumbTint">@color/colorForeground</item>
        <item name="trackTint">@color/colorForeground</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/colorSwitchThumbNormal</item>


    </style>


</resources>