/* * To change this license header, choose License Headers in Project Properties. * To change this template file, choose Tools | Templates * and open the template in the editor. */package eu.pp.cashwizard.model;import java.math.BigDecimal;import eu.pp.cashwizard.dict.Currency;import eu.pp.cashwizard.exceptions.DataException;import lombok.EqualsAndHashCode;import lombok.Getter;import lombok.Setter;/** * * @author Majkel */@Getter@Setterpublic class PersonAmount {    Person person;    BigDecimal amount;    public PersonAmount(Person person, BigDecimal amount) {        this.person = person;        this.amount = amount;    }    public int compareTo( PersonAmount pa2 ) throws DataException {        if( pa2 == null || pa2.getAmount() == null ) throw new DataException();        if( person == null || amount == null ) throw new DataException();        return amount.compareTo( pa2.amount );    }//    public boolean equals( PersonCurrency o ) {//        //    }    }